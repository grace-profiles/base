description: Adds support for Markup Views to the project
build:
    plugins:
        - org.graceframework.plugins.views-markup
dependencies:
    - scope: build
      coords: "org.graceframework.plugins:views-gradle:5.1.1"
    - scope: compile
      coords: "org.graceframework.plugins:views-markup"
