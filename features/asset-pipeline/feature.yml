description: Adds Asset Pipeline to a Grace project
build:
    plugins:
        - org.graceframework.asset-pipeline
dependencies:
    - scope: build
      coords: 'org.graceframework.plugins:asset-pipeline-gradle:5.0.0'
    - scope: runtime
      coords: "org.graceframework.plugins:asset-pipeline-plugin:5.0.0"
