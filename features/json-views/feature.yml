description: Adds support for JSON Views to the project
build:
    plugins:
        - org.graceframework.plugins.views-json
dependencies:
    - scope: build
      coords: "org.graceframework.plugins:views-gradle:5.0.0"
    - scope: compile
      coords: "org.graceframework.plugins:views-json"
    - scope: compile
      coords: "org.graceframework.plugins:views-json-templates"
